
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Image } from '../components/image/image';
import { Backdrop } from '../components/backdrop/backdrop';

<Meta title="Components/Utility/Backdrop" component={Backdrop} />

export const Template = (args) =><div className="h-screen"><Image styling="p-12 text-white text-center text-h3"><span className="relative z-20">Lorem Ipsum</span><Backdrop {...args} /></Image></div>;

# Backdrop

<Canvas>
    <Story 
    name="Default"
    args={{
      color: 'black',
      opacity: '50',
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
