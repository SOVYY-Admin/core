
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { IMG } from '../components/img/img';
import { Backdrop } from '../components/backdrop/backdrop';

<Meta title="Components/Utility/Backdrop" component={Backdrop} />

export const Template = (args) =><div className="h-screen"><IMG styling="p-12 text-white text-center text-h3"><span className="relative z-20">Lorem Ipsum</span><Backdrop {...args} /></IMG></div>;

# Backdrop

<Canvas>
    <Story 
    name="Default"
    args={{
      color: 'black',
      opacity: '50',
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
