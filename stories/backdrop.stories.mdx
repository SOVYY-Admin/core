
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { IMG } from '../components/img/img';
import { Backdrop } from '../components/backdrop/backdrop';

<Meta title="Components/Utility/Backdrop" component={Backdrop} />

export const Template = (args) =><div className="h-screen"><IMG styling="z-n" /><Backdrop {...args} /></div>;

# Backdrop

<Canvas>
    <Story 
    name="Default"
    args={{
      color: 'black',
      opacity: 'o50',
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
