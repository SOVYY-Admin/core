
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { IMG } from '../components/img/img';
import { Backdrop } from '../components/components';

<Meta title="Components/Utility/Backdrop" component={Backdrop} />

export const Template = (args) =><Backdrop {...args} styling="h-screen"/>;

# Backdrop

<Canvas>
    <Story 
    name="Default"
    args={{
      color: 'black',
      opacity: 'o50',
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
