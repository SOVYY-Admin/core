
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Grid } from '../components/components';

<Meta 
  title="Components/Layout/Grid" 
  component={Grid}
  argTypes={{
    numberOfChildren: { type: "number", defaultValue: 12 },
  }}
/>

export const Template = ({ numberOfChildren, ...args }) => 
<Grid {...args}> 
   {[...Array(numberOfChildren).keys()].map(n => (
    <div className="bg-blue-500 p-8 text-center text-white">{n + 1}</div>
    ))}
</Grid>;

# Grid

<Canvas>
    <Story 
    name="Default"
    args={{
      cols: '4',
      gap: '2',
      styling: '',
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
