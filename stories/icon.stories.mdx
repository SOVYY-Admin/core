
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Icon } from '../components/icon/icon';

<Meta title="Components/DataDisplay/Icon" component={Icon} />

export const Template = (args) => <Icon {...args} />;

# Icon

<Canvas>
    <Story 
    name="Default"
    args={{
      size: 'medium',
      icon: 'image',
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
